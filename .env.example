# =============================================================================
# CONFIGURAÇÃO DO BANCO DE DADOS - CONTA COMIGO
# =============================================================================
# Este arquivo contém as variáveis de ambiente necessárias para o projeto.
# Copie este arquivo para .env e ajuste os valores conforme necessário.

# -----------------------------------------------------------------------------
# CONFIGURAÇÃO DO POSTGRESQL PARA DOCKER
# -----------------------------------------------------------------------------
# Estas variáveis são usadas pelo Docker Compose para criar o container do PostgreSQL

# Usuário administrador do PostgreSQL
POSTGRES_USER=postgres

# Senha do usuário administrador (ALTERE em produção!)
POSTGRES_PASSWORD=postgres

# Nome do banco de dados que será criado
POSTGRES_DB=contacomigodb

# Porta externa do PostgreSQL (evita conflito com instalações locais)
# Porta padrão do PostgreSQL é 5432, mas usamos 1230 para evitar conflitos
POSTGRES_PORT=1230

# -----------------------------------------------------------------------------
# CONFIGURAÇÃO DA APLICAÇÃO NODE.JS/SEQUELIZE
# -----------------------------------------------------------------------------
# Estas variáveis são usadas pela aplicação para conectar ao banco de dados

# Endereço do servidor do banco (localhost para desenvolvimento)
DB_HOST=localhost

# Porta do banco (deve ser a mesma do POSTGRES_PORT)
DB_PORT=1230

# Nome do banco (deve ser o mesmo do POSTGRES_DB)
DB_NAME=contacomigodb

# Usuário para conexão (deve ser o mesmo do POSTGRES_USER)
DB_USER=postgres

# Senha para conexão (deve ser a mesma do POSTGRES_PASSWORD)
DB_PASS=postgres

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES ADICIONAIS (OPCIONAL)
# -----------------------------------------------------------------------------

# Porta da aplicação web
PORT=3000
